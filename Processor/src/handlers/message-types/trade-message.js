module.exports = async function handleTradeMessage(
  { msgType, nanos, matchId, comboGroupId, side, quantity, orderBookId,
    price, owner, counterparty, printable, cross, sequenceNo },
  rawMessage
) { /* TODO */ }
