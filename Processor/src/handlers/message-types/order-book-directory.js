module.exports = async function handleOrderBookDirectory(
  { msgType, nanos, orderBookId, symbol, longName, isin, financialProduct,
    tradingCurrency, decimalsInPrice, decimalsInNominalValue, roundLotSize,
    nominalValue, numberOfLegs, underlyingOrderBookId, strikePrice,
    expirationDate, decimalsInStrikePrice, optionType, decimalsInQuantity,
    testOrderbook, sequenceNo },
  rawMessage
) { /* TODO */ }
